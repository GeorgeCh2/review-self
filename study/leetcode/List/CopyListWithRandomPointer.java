public class CopyListWithRandomPointer {
    public Node copyRandomList(Node head) {
        Node dummy = new Node();
        while(null != head) {
            dummy.val = head.val;
            
        }
    }
}